<Window x:Class="Niero.LoadingWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Niero"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        
        mc:Ignorable="d"
        
        Height="350" Width="300"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent">

    <Window.Resources>
        
    </Window.Resources>

    <Grid x:Name="WelcomeWindowGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.2*"/>
        </Grid.RowDefinitions>
        <Image x:Name="Start_gif" Height="230" Width="230" gif:ImageBehavior.AnimatedSource="../Gifs/Main.gif"/>
        <Button x:Name="Start_Button" 
                Template="{DynamicResource DefaultButtonTemplate}" 
                Style="{DynamicResource StartButton}" 
                Content="Start making magic..." 
                Grid.Row="1"
                VerticalAlignment="Center"/>
    </Grid>
</Window>
